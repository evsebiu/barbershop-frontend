import React from 'react';

const Hero = () => {
  return (
    <section className="hero">
      <div className="hero-overlay"></div>
      <div className="hero-content">
        <h1>EXCLUSIV PENTRU BĂRBAȚI</h1>
        <p className="hero-subtitle">Experimentează tunsul la nivel de artă.</p>
        <a href="#barbers" className="btn-cta">
          Accesează Frizerii
        </a>
      </div>
    </section>
  );
};

export default Hero;